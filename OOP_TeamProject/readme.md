
- 일반 클래스 필드 2개 이상, 속성(Property), 생성자 1개 이상 포함 10개 이상  
- 구조체(struct) 값 타입의 적절한 사용 1개 이상  
- 메서드 매개변수 / 반환값을 가진 메서드 다양하게 30개 이상  
- 메서드 오버로딩 같은 이름, 다른 시그니처 3쌍 이상  
- out 키워드 값을 여러 개 반환하는 메서드 2개 이상  
- 상속 계층  부모-자식 관계 (3단 이상 권장) 2개 이상  
- 메서드 오버라이딩  virtual / override 사용 5개 이상  
- base 키워드  부모 클래스 멤버 / 생성자 호출 3회 이상  
- protected 멤버  접근 제한자 적절히 사용 필수  
- 추상 클래스  abstract 클래스 / 메서드 정의 및 구현 1개 이상  
- 인터페이스  직접 정의한 인터페이스 + 구현 3개 이상  
- 표준 인터페이스  IComparable 또는 IDisposable 구현 1개 이상  
- 다중 인터페이스 상속  한 클래스가 2개 이상 인터페이스 구현 1개 이상  
- 제네릭  직접 정의한 제네릭 클래스 또는 메서드 1개 이상  
- 인덱서  [ ] 연산자로 접근 가능한 인덱서 1개 이상  
- try-catch-finally  실제로 의미 있는 예외 처리 5회 이상  
- 사용자 정의 예외  Exception 클래스를 상속한 예외 1개 이상  
- throw 사용  예외 강제 발생 필수  
- 델리게이트  직접 정의한 델리게이트 + 사용 1개 이상  
- 람다식  정렬, 콜백, 필터 등 의미 있는 람다 사용 5회 이상  
- 컬렉션 활용  List<T>, Dictionary<K,V> 등 적절한 사용 필수  

< 정성적 요구사항 >  
• 단순히 개수만 채우지 말고, 각 개념이 도메인 안에서 의미 있게 사용되어야 함.  
• 클래스명·메서드명·변수명은 일관된 네이밍 규칙(Pascal/camel case)을 따른다.  
• 한 메서드가 100줄을 넘지 않도록 적절히 분리한다.  
• 필드는 가능한 private으로, 외부 접근은 속성(Property)을 통해서 한다 (캡슐화).  
• 의미 없는 빈 catch { } (예외 무시) 는 감점 대상이다.  
